# CJHX Agile Workflow Web 使用指南

本指南说明如何通过 CJHX Agile Workflow Web 控制台，治理并执行一个项目从目标设定、需求与技术设计、任务拆解、Agent 编码、评审验证到发布验收的完整研发流程。

> CJHX 不是代码编辑器，也不是“一键自动完成项目”的工具。它是研发控制面：用 `Workspace → Goal → Change → Task → Agent Run / Session → Harness → Evidence` 将设计、开发、验证和审批连接成一条可审计的工作流。

---

## 1. 核心对象与关系

```text
Workspace（代码仓库）
  ↓
Goal（为什么做、怎样算成功）
  ↓
Change（一次完整研发交付）
  ↓
Task（可独立执行和验收的工作）
  ↓
Agent Run / Session（设计、编码和修正）
  ↓
Harness（自动化检查与任务门禁）
  ↓
Evidence（需求、设计、评审、验证、发布证据）
```

| 对象 | 作用 |
|---|---|
| Workspace | 绑定本地 Git 仓库或远端虚拟仓库，是代码和执行范围 |
| Goal | 定义预期结果、成功标准、范围、约束和事实来源 |
| Change | 编排一次交付的十二阶段生命周期 |
| Task | 承载具体工作、验收标准、依赖和状态流转 |
| Agent Run | 一次性完成一个边界清晰的 Task |
| Agent Session | 围绕一个 Task 进行可恢复的多轮设计或开发对话 |
| Harness | 在 Agent 执行前后实施规则快照、检查和 Task 门禁 |
| Evidence | 指向 Jira、Confluence、代码托管、DevOps 或其他权威事实 |

Task 状态与 Change 生命周期彼此独立：Task 进入“评审中”不会自动推动 Change 进入“代码评审”，Change 推进也不会隐式改写 Task 状态。

---

## 2. 启动 Web 控制台

### 2.1 从源码仓库启动

要求 Node.js 20+。

```bash
cd /Users/xxx/cjhx-agile-workflow
npm install --include=dev
npm run ui
```

默认地址：

```text
http://127.0.0.1:4317
```

保持启动 Web UI 的终端进程运行。Agent Run 应从这个正在运行的 Web UI 发起，以便拥有运行记录的 CJHX 进程持续存活并完成 Harness postflight 检查。

### 2.2 使用已安装的 CLI 启动

```bash
cjhx --workspace .cjhx ui
```

常用参数：

```text
--host 127.0.0.1   仅允许回环地址
--port 4317        指定端口
--no-open          不自动打开浏览器
```

Web UI 是本地控制面，不应直接暴露到公网或通过反向代理作为多人生产系统使用。

---

## 3. 首次使用前准备项目仓库

### 3.1 创建或确认本地 Git 仓库

Agent 开发目前只支持本地 Workspace，因此目标项目必须是本地 Git 仓库。

```bash
mkdir -p /Users/xxx/my-project
cd /Users/xxx/my-project
git init
```

建议项目至少包含：

```text
my-project/
├── README.md
├── package.json
├── cjhx.harness.json
├── src/
└── tests/
```

远端 GitHub/GitLab 虚拟 Workspace 可用于实时浏览仓库、Issue 和 PR/MR，但不能直接启动本地 Agent 写代码。

### 3.2 配置项目检查命令

以 Node/TypeScript 项目为例：

```json
{
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "check": "npm run typecheck && npm test"
  }
}
```

项目命令必须能够在仓库根目录直接执行，并以退出码 `0` 表示成功。

### 3.3 配置 Harness

在目标项目根目录创建 `cjhx.harness.json`：

```json
{
  "schemaVersion": 1,
  "id": "project-engineering",
  "version": "1.0.0",
  "scope": "workspace",
  "mode": "enforce",
  "rules": [
    {
      "id": "task-quality",
      "description": "进入验证前必须有验收标准并通过项目检查",
      "instruction": "采用最小安全改动，添加测试，并总结验证结果。",
      "preconditions": [
        "workspace.is-local",
        "task.has-acceptance-criteria"
      ],
      "requiredChecks": [
        "npm.check"
      ],
      "gates": [
        {
          "target": "task.verification",
          "requires": [
            "check:npm.check"
          ]
        }
      ]
    }
  ]
}
```

当前批准的检查目录包括：

- `npm.typecheck`
- `npm.test`
- `npm.check`

Harness 不接受任意 Shell 命令。它只执行批准目录中的检查，Prompt 指令也不能伪装成执行器真正强制的能力。

可在 CJHX 仓库中验证规则示例：

```bash
cd /Users/xxx/cjhx-agile-workflow
cjhx harness-validate /Users/xxx/my-project/cjhx.harness.json
```

---

## 4. 导入 Workspace

1. 打开 Web 控制台。
2. 左侧点击 **“工作空间”**。
3. 点击 **“导入 Workspace”**。
4. Workspace 类型选择 **“本地 Git 仓库”**。
5. 填写：
   - 显示名称，例如 `my-project`；
   - 本地仓库路径，例如 `/Users/xxx/my-project`。
6. 点击 **“导入 Workspace”**。

导入后可使用以下视图：

| 视图 | 用途 |
|---|---|
| Overview | 查看仓库和工作概况 |
| Kanban | 管理当前 Workspace 的统一任务看板 |
| Sessions | 查看 Task 绑定的 Agent 会话 |
| Team | 查看 Owner 负载 |
| Codebase | 浏览目录、代码、Git refs、提交和 Worktree |

Workspace Kanban 使用七状态模型。`Backlog` 固定在左侧，`已完成`固定在右侧，只有中间五个执行状态横向滚动。

---

## 5. 配置 Pi Agent

1. 左侧点击 **“智能体”**。
2. 如果已存在 `Pi · 默认`，可以直接使用。
3. 否则点击 **“添加智能体”**。
4. Agent 类型选择 **Pi**。
5. 使用以下推荐配置：

```text
配置 ID：pi
显示名称：Pi
可执行命令：pi
Prompt 传递方式：命令参数
超时：30 分钟
```

执行参数：

```json
[
  "--no-extensions",
  "-p",
  "{prompt}"
]
```

版本测试参数：

```json
[
  "--version"
]
```

6. 勾选“已批准执行该 Agent 的版本测试命令”。
7. 点击 **“测试并保存”**。
8. 如果配置了多个 Agent，将 Pi 设为默认。

`--no-extensions` 可避免 Pi 非交互运行完成后被扩展持有的连接阻塞退出。

如需人工确认，可以在 Agent 卡片上点击 **“终端验证”**。CJHX 会先执行版本测试，再打开交互式终端。

---

## 6. 创建项目 Goal

Goal 表示“为什么做”和“怎样算成功”，不是具体编码任务。

### 6.1 创建 Goal 草稿

1. 左侧点击 **“目标”**。
2. 点击 **“设置目标”**。
3. 填写：
   - Workspace；
   - 目标标题；
   - Owner；
   - 优先级；
   - 目标声明；
   - 为什么现在做；
   - 目标日期；
   - 复盘周期；
   - 范围内；
   - 范围外；
   - 约束与非目标；
   - 成功标准；
   - 事实源引用。

示例：

```text
目标标题：
交付客户反馈管理 MVP

目标声明：
让内部用户能够创建、分类、搜索和跟踪客户反馈。

为什么现在做：
当前反馈散落在聊天和表格中，无法追踪处理状态。

范围内：
- 创建反馈
- 分类和优先级
- 状态流转
- 搜索
- 本地持久化

范围外：
- 企业 SSO
- 多租户
- SaaS 部署

约束：
- 本地优先
- TypeScript
- 所有核心行为必须有自动化测试
```

成功标准应可验证，例如：

```text
1. 核心行为测试全部通过
2. 用户可以创建并查询反馈
3. 刷新后数据不会丢失
4. npm run check 返回 0
```

每个成功标准都应填写事实来源和验证方式。不要只写“功能完成”。

### 6.2 激活 Goal

保存后得到 Goal 草稿。准备度满足后打开 Goal，点击 **“激活目标”**。

Goal 激活、编辑和状态变化都会生成不可变 GoalSnapshot。Goal 的健康状态由成功标准、阻塞 Task 和自动化 Finding 确定性投影，不能由 Agent 的口头声明直接改变。

---

## 7. 创建并关联 Change

Change 表示围绕一个目标开展的一次完整研发交付。

### 7.1 创建 Change

1. 左侧点击 **“研发流程”**。
2. 点击 **“创建变更”**。
3. 填写：
   - 关联 Workspace；
   - Change ID；
   - Owner；
   - 标题；
   - 风险等级；
   - 说明。

示例：

```text
Change ID：FB-1
标题：客户反馈管理 MVP
Owner：product-owner
风险等级：L2
说明：实现反馈创建、分类、查询和状态流转
```

创建后 Change 的初始状态是：

```text
意图草拟 intent_draft
```

### 7.2 将 Change 关联回 Goal

1. 回到 **“目标”**。
2. 打开之前创建的 Goal。
3. 点击 **“编辑目标”**。
4. 在“关联 Change”中勾选 `FB-1`。
5. 保存。

Goal 只能关联同一个 Workspace 下的 Change，因此二者必须选择一致的 Workspace。

---

## 8. 使用 Agent Session 进行产品与技术设计

Agent Session 适合多轮、需要逐步收敛的工作：

- 需求澄清；
- 领域建模；
- API 和模块接口设计；
- 技术方案比较；
- 任务拆解；
- 设计评审与修订。

Session 必须绑定 Task，因此需要先创建设计 Task。

### 8.1 创建设计 Task

进入：

```text
工作空间 → 选择 Workspace → Kanban → 新建任务
```

填写示例：

```text
关联 Workspace：my-project
关联变更：FB-1
任务标题：设计反馈领域模型和技术方案
Owner：architect
优先级：P1
风险等级：L2

任务说明：
分析反馈实体、分类、状态流转、存储边界和 API 接口，并形成可实施方案。

验收标准：
- 明确定义核心实体和状态
- 说明存储方案及取舍
- 定义模块边界和主要接口
- 列出关键风险
- 输出可拆分的实现任务
```

验收标准每行一条。没有验收标准的 Task 会被推荐的 Harness precondition 拒绝。

### 8.2 创建并运行设计会话

1. 点击 Task 卡片。
2. 点击 **“会话”**。
3. 点击 **“新建会话”**。
4. 输入会话标题，例如“反馈管理 MVP 技术设计”。
5. 点击 **“继续会话”**。
6. 选择 Pi。
7. 输入本轮请求。

第一轮示例：

```text
请先阅读当前代码仓库、README 和项目约束。

为客户反馈管理 MVP 制定技术设计，但暂时不要修改代码。

输出：
1. 需求理解和未决问题
2. 领域模型
3. 模块边界
4. 数据存储方案
5. UI 信息架构
6. 测试策略
7. 风险和取舍
8. 建议的实现任务拆分
```

点击 **“预览执行上下文”** 后，检查：

- Execution Context 摘要；
- Memory Snapshot；
- Harness 规则；
- 最近 Turns；
- 长期记忆；
- 实际组装的 Prompt。

确认无误后勾选审批，点击 **“批准并启动”**。

第二轮示例：

```text
基于上一轮方案，采纳方案 B。

请进一步明确：
1. 模块公开接口
2. 数据模型字段
3. 状态转换规则
4. 错误处理策略
5. 每个实现任务的验收标准

仍然不要修改代码。
```

第三轮可以要求落地设计文档：

```text
请将已确认的方案写入 docs/ARCHITECTURE.md，
并将关键决策写入 docs/adr/0001-feedback-storage.md。
完成后运行文档相关检查，但不要开始产品功能编码。
```

Session 可跨 Agent 继续。历史内容只作为非指令参考；当前 Task、Policy、Harness 和本轮请求优先。

---

## 9. 使用 Skill 与声明式 Workflow

Change 页面右侧的 **Agentic Actions** 提供：

- **运行 Skill**；
- **运行 Workflow**。

这里的 Workflow 是 CJHX 的声明式多 Skill 编排，不是“一键执行整个 SDLC”。它需要：

- 已安装并启用的 Skills；
- Workflow JSON 定义；
- Workflow 输入 JSON；
- 正确的 `$ref` 数据流。

典型用途：

```text
需求拆解 → 测试用例生成
```

示例：

```json
{
  "id": "requirement-to-tests",
  "version": "1.0.0",
  "name": "需求到测试",
  "steps": [
    {
      "id": "decompose",
      "skill": "requirement.decompose",
      "input": {
        "requirement": {
          "$ref": "input.requirement"
        }
      }
    },
    {
      "id": "test-cases",
      "skill": "test.case.generate",
      "input": {
        "feature": {
          "$ref": "input.feature"
        },
        "acceptanceCriteria": {
          "$ref": "input.acceptanceCriteria"
        },
        "tasks": {
          "$ref": "steps.decompose.output.tasks"
        }
      }
    }
  ]
}
```

运行完成后：

1. 左侧进入 **“运行记录”**；
2. 打开成功的需求拆解运行；
3. 点击 **“将拆解任务添加到看板”**；
4. 或在 Workspace Kanban 顶部点击 **“导入拆解任务”**。

如果尚未安装相应 Skill，优先使用手工 Task + Agent Session，流程更直接。

---

## 10. 拆分实现 Task

设计确认后，将工作拆成小而可验证的 Task。例如：

```text
任务 1：建立领域模型和存储层
任务 2：实现反馈创建与校验
任务 3：实现分类和状态流转
任务 4：实现查询和搜索
任务 5：实现 UI 和端到端验证
```

每个 Task 都应填写：

- 关联 Workspace；
- 关联 Change；
- Owner；
- 优先级；
- 风险等级；
- 任务说明；
- 验收标准；
- 依赖关系；
- 已知证据引用。

依赖示例：

```text
任务 2 依赖任务 1
任务 3 依赖任务 1
任务 4 依赖任务 1
任务 5 依赖任务 2、3、4
```

一个 Task 应尽量能由一次 Agent Run 完成并独立验证。范围过大时继续拆分，不要把整个项目放进一个 Task。

---

## 11. 启动一次性 Agent 编码

一次性 Agent Run 适合边界明确的编码 Task。

### 11.1 启动前

1. 打开 Workspace Kanban。
2. 将 Task 从 **“待开始”** 更新到 **“进行中”**。
3. 点击 Task 卡片。
4. 点击 **“一次性 Agent 运行”**。

### 11.2 审批并启动

1. 选择 Pi。
2. 填写补充开发指令。
3. 检查 Harness 摘要：
   - Preflight 是否通过；
   - Rule Snapshot digest；
   - Required Checks 是否正确；
   - Task Gates 是否包含 `task.verification`；
   - 执行器是否真实支持规则要求的能力。
4. 勾选“已审阅并批准上述 Harness 规则摘要下的 Agent 任务执行”。
5. 点击 **“批准并启动”**。

推荐补充指令：

```text
请使用测试驱动方式完成本任务。

要求：
1. 只实现当前 Task 的范围
2. 不修改无关文件
3. 先阅读项目文档和现有测试
4. 添加或更新自动化测试
5. 运行 npm run check
6. 不 push、不发布、不部署
7. 最后总结改动文件、测试结果和剩余风险
```

CJHX 会让 Agent 在 Task 关联的本地 Workspace Git 根目录运行。Agent 结束后，CJHX 自动执行 Harness postflight checks，并分别记录：

- Agent 进程状态；
- Harness 合规状态；
- 规则快照摘要；
- 每项检查及退出码；
- 标准输出和错误输出。

Agent 进程成功不等于 Harness 合规通过。

---

## 12. Task 状态流转

Task 使用以下七状态：

```text
Backlog
→ 待开始
→ 进行中
→ 评审中
→ 验证中
→ 已完成
```

另有可从多个阶段进入的：

```text
已阻塞
```

推荐节奏：

### 12.1 Backlog

范围、依赖或验收标准尚未澄清时保持在 Backlog。完成澄清后：

```text
Backlog → 待开始
```

### 12.2 进行中

启动 Agent 或人工开发前：

```text
待开始 → 进行中
```

### 12.3 评审中

Agent 完成后不要直接完成，先进入：

```text
进行中 → 评审中
```

检查：

- Agent 输出；
- Git diff；
- 测试覆盖；
- 验收标准；
- 是否修改了无关文件；
- 是否存在未解决风险；
- 是否需要返工。

评审必须是一个真实阶段。不要在自动化脚本中用毫秒级连续转换把“评审中”直接跳过。

### 12.4 验证中

评审通过后：

```text
评审中 → 验证中
```

如果 Harness 对 `task.verification` 配置了门禁，只有当前仓库状态对应的 Compliance Report 通过后才能进入验证。Agent 运行后再次修改代码会使旧报告过期，需要重新验证。

### 12.5 已完成

人工确认验收标准满足后：

```text
验证中 → 已完成
```

### 12.6 已阻塞

出现外部依赖、设计未决、环境不可用或证据缺失时，将 Task 更新为“已阻塞”，并在原因中记录事实和解除条件。

---

## 13. Change 十二阶段生命周期

| 阶段 | 状态 ID | 进入前需要的证据 |
|---|---|---|
| 意图草拟 | `intent_draft` | 初始状态 |
| 意图确认 | `intent_confirmed` | `intent-approval` |
| 需求就绪 | `requirement_ready` | `requirement-spec` |
| 设计批准 | `design_approved` | `technical-design`、`design-approval` |
| 开发实现 | `implementing` | 无新增证据 |
| 代码评审 | `reviewing` | `change-request` |
| 质量验证 | `verified` | `code-review`、`quality-verification` |
| 业务验收 | `accepted` | `acceptance-approval` |
| 发布批准 | `release_approved` | `release-plan`、`release-approval` |
| 部署中 | `deploying` | 无新增证据 |
| 运行中 | `operating` | `deployment-record` |
| 结果验证 | `outcome_validated` | `outcome-validation` |

### 13.1 添加证据

1. 左侧点击 **“研发流程”**。
2. 选择 Change。
3. 查看右侧 **“阶段操作”**。
4. 如果转换按钮显示缺少证据，点击 **“添加证据”**。
5. 填写：
   - 证据类型；
   - 状态：`approved`、`passed` 或 `valid`；
   - 来源平台；
   - 事实引用；
   - 可选 URI。

设计证据示例：

```text
证据类型：technical-design
状态：approved
来源平台：confluence
事实引用：confluence://FB-1/technical-design
```

设计审批示例：

```text
证据类型：design-approval
状态：approved
来源平台：jira
事实引用：jira://FB-1/design-approval
```

如果暂未连接 Jira 或 Confluence，可以引用真实存在的本地文档或 Git commit，但引用必须可追溯，不要伪造平台事实。

CJHX 保存的是证据索引，不复制完整 Jira、Confluence、DevOps 或代码托管业务数据。

### 13.2 推进阶段

证据满足后：

1. 点击“推进至……”；
2. 填写执行人；
3. 填写转换原因；
4. 确认转换。

转换会写入不可变历史。门禁不满足时 UI 和后端都会拒绝推进。

---

## 14. 推荐的完整端到端顺序

### 14.1 项目初始化

```text
1. 创建本地 Git 仓库
2. 添加项目检查命令
3. 添加 cjhx.harness.json
4. 启动 CJHX Web
5. 导入本地 Workspace
6. 配置并测试 Pi Agent
```

### 14.2 规划与设计

```text
7. 创建 Goal 草稿
8. 创建 Change
9. 将 Change 关联回 Goal
10. 完善并激活 Goal
11. 添加 intent-approval
12. Change → 意图确认
13. 创建需求/设计 Task
14. 用 Agent Session 多轮完成需求与技术设计
15. 记录 requirement-spec
16. Change → 需求就绪
17. 记录 technical-design + design-approval
18. Change → 设计批准
```

### 14.3 开发

```text
19. 拆分实现 Task，填写验收标准和依赖
20. Change → 开发实现
21. Task → 进行中
22. 启动一次性 Agent Run 或 Agent Session
23. 检查 Agent Run 和 Harness Compliance Report
24. Task → 评审中
```

### 14.4 评审与验证

```text
25. 进行真实的人工或 Agent 代码评审
26. 创建真实代码变更请求并添加 change-request 证据
27. Change → 代码评审
28. 评审通过后 Task → 验证中
29. 添加 code-review + quality-verification 证据
30. Change → 质量验证
31. 人工确认验收标准后 Task → 已完成
```

### 14.5 验收与发布

```text
32. 添加 acceptance-approval
33. Change → 业务验收
34. 添加 release-plan + release-approval
35. Change → 发布批准
36. 通过 DevOps 触发部署
37. Change → 部署中
38. 添加 deployment-record
39. Change → 运行中
40. 添加 outcome-validation
41. Change → 结果验证
42. 所有 Goal 成功标准满足后，Goal → 已达成
```

---

## 15. DevOps 与集成配置

左侧 **“集成配置”** 可以连接：

- Jira：任务发布、状态同步和回写；
- DevOps：流水线、制品、部署和服务控制；
- GitLab 或 GitHub：仓库、分支、提交和 PR/MR 投影。

凭据只保存在本机 `.cjhx` 控制面文件中，不会回显到浏览器。

配置 Jira 后，本地 Task 可以显式审批并发布到 Jira。发布后 Jira 成为该 Task 状态的权威来源。

配置 DevOps 后，可以在 **“DevOps”** 页面查看流水线和服务，并在填写执行人、原因和人工审批后触发 CI/CD 或服务操作。

Agent 默认不得自行 push、发布或部署。代码托管和 DevOps 写操作应通过明确的人工审批和相应 Adapter 执行。

---

## 16. 自动化工程审计

左侧点击 **“自动化”**，可创建每日或工作日仓库审计，检查：

- 依赖变化；
- 测试失败；
- 高风险变更；
- 发布健康；
- 卡住的 Task。

自动化是只读的。它不会：

- 修改 Task 状态；
- 写回 Jira；
- 触发流水线；
- 修改代码托管状态；
- 写入长期 Memory。

报告绑定仓库状态。代码发生变化后，历史报告可能被标记为 stale。

---

## 17. Memory 使用原则

Task 详情中的 **“记忆”** 用于保存已经确认、未来可复用的信息：

- 决策；
- 约束；
- 偏好；
- 经验；
- 待确认问题。

长期记忆必须由用户显式保存、纠正、固定或遗忘。它不能替代：

- Jira 状态；
- Confluence 需求或设计；
- Harness 检查；
- 生命周期 Evidence；
- DevOps 执行事实。

保存具体、稳定、可复用的信息，不要把临时进度或普通 TODO 写入长期记忆。

---

## 18. 当前产品边界

使用时需要了解以下边界：

1. 当前 Web UI 是 loopback-only 本地控制面，不是多人生产网关。
2. 本地 Agent CLI 与操作系统用户拥有相同权限，不是真正的安全沙箱。
3. Agent 开发只支持本地 Workspace。
4. GitHub/GitLab 虚拟 Workspace 是实时只读投影。
5. CJHX 不会自动跳过 Evidence 或 Harness 门禁。
6. Agent Run 成功不等于 Harness 合规通过。
7. 通过后的 Compliance Report 绑定仓库状态；后续改动会使其过期。
8. `运行 Workflow` 是多 Skill 编排，不是自动完成整个项目的按钮。
9. Task 状态与 Change 生命周期相互独立。
10. 自动化审计只读，不能替代人工审批或权威平台事实。
11. Git push、发布和部署不应交给本地 Agent 隐式执行。

企业生产使用应提供独立网关、身份认证、RBAC、集中密钥管理、数据库、队列，以及容器、gVisor/Kata 或微虚机隔离执行器。

---

## 19. 最短启动路径

如果要立即启动一个新项目，按以下顺序点击：

```text
1. 工作空间 → 导入本地 Git 仓库
2. 智能体 → 确认 Pi 为默认 Agent
3. 目标 → 设置 Goal 草稿
4. 研发流程 → 创建 Change
5. 目标 → 编辑 Goal → 关联 Change → 激活
6. 工作空间 → Kanban → 新建“需求与技术设计”Task
7. 打开 Task → 会话 → 新建会话
8. 继续会话 → 预览执行上下文 → 批准并启动
9. 设计确认后拆分实现 Task
10. Task → 进行中 → 一次性 Agent 运行
11. 检查 Agent Run 和 Harness
12. Task → 评审中 → 验证中 → 已完成
13. 在研发流程中按证据门禁推进 Change
14. 成功标准全部满足后确认 Goal 达成
```

CJHX 的工作流程不是由一个总开关启动，而是由 Goal、Change、Task、Agent Session/Run、Harness 和 Evidence 共同构成。对于新项目，**第一个真正的执行入口通常是绑定到本地 Workspace 和 Change 的“需求与技术设计”Task 会话**。

---

## 20. 相关文档

- [README](README.md)
- [Web UI 与安全边界](docs/UI.md)
- [架构与系统边界](docs/ARCHITECTURE.md)
- [Harness Engineering](docs/HARNESS_ENGINEERING.md)
- [Skill 开发与治理](docs/SKILLS.md)
- [会话与记忆](docs/MEMORY.md)
- [平台集成](docs/INTEGRATIONS.md)
